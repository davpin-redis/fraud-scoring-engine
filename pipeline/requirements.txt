# Feedback-loop data/ML pipeline (Python side; the engine stays Java).
redis>=5.0
pyarrow>=14.0
lightgbm>=4.0        # Track A model (Phase 2)
onnxmltools>=1.12    # LightGBM -> ONNX export for in-engine scoring (Phase 4/B4)
scikit-learn>=1.3    # metrics / PR-AUC in the eval harness (Phase 2)
fastapi>=0.110       # metrics aggregator SSE (Phase 3/C1)
uvicorn>=0.29
pytest>=8.0
